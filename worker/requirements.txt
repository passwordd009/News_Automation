# Phase 1 — core runtime dependencies
requests>=2.32
feedparser>=6.0
beautifulsoup4>=4.12
pydantic>=2.7
SQLAlchemy>=2.0
python-dotenv>=1.0

# Phase 6+ — Google Docs output (not imported yet, safe to install early)
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2

# Development
pytest>=8.0
